## Summary

## Validation

- [ ] `npm run check`
- [ ] `npm test`
- [ ] `npm run test:visual` when rendering changed
- [ ] Visual artifact reviewed when rendering changed
- [ ] New executable tasks are registered in `package.json` and GitHub Actions

## Merge policy

This pull request must be **squash merged** into `main` after approval and green CI.
